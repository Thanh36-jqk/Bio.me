# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017
# For MongoDB Atlas (cloud):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/biometric_mfa?retryWrites=true&w=majority

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# Model Paths (optional, defaults to models/)
FACE_MODEL_PATH=models/face
IRIS_MODEL_PATH=models/iris
FINGERPRINT_MODEL_PATH=models/fingerprint

# Security (for future JWT implementation)
SECRET_KEY=your-secret-key-change-this-in-production
ALGORITHM=HS256
